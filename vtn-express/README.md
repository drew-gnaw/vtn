# VTN-Express

Welcome to this short documentation for `vtn-express`, 
a lightweight backend that allows manipulation of resources.

### Setup

Make sure your code is up to date (`git fetch` and `git pull`), and
`cd vtn-express` to move into the folder.

Run `npm i` to install required packages.

Run `vercel dev` to start running the backend locally.